<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kingsgdl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.kingsgdl.StaticContextFactory"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KingsGDL">

<!--        <activity android:name="." />-->
        <activity android:name=".kings.tablelayout.MainActivityTable" />
        <activity android:name=".kings.tablelayout.PhotoView" />
        <activity android:name=".kings.MainActivityKings" />
        <activity android:name=".listofstates.StatesMainActivity" />
        <activity android:name="drawer.MainActivityDrawer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>